[pack]
name   = "adr-pipeline"
schema = 2
version = "0.3.0"
description = "ADR-driven development pipeline using the dog pool for all roles"

# --- Mayor (always-on orchestrator) ---

[[named_session]]
template = "mayor"
scope    = "city"
mode     = "always"
